services:
    senky.userstatistics.listener:
        class: senky\userstatistics\event\listener
        arguments:
            - @cache.driver
            - @dbal.conn
            - @template
            - @user
        tags:
            - { name: event.listener }
